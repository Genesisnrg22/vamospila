<h1>Estás en el index de tu página</h1>

<% @services.each do |service| %>
  <div class="mt-3">
    <h2><%= service.title %></h2>
    <p>Dueño: <%= service.user.first_name %></p>
    <%= cl_image_tag service.photos.first.key, height: 300, width: 400, crop: :fill %>
    <div>
      <%= link_to "Ver más", service_path(service), class:"btn btn-success" %>
    </div>
  </div>
<% end %>
